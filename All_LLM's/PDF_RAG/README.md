# 📄 Chat with PDF using DeepSeek

RAG application to chat with PDF documents using DeepSeek API.

## 🚀 Quick Start

1. **Install dependencies**
   ```bash
   pip install -r requirements.txt
   ```

2. **Set API key in `.env` file** (root directory)
   ```env
   DEEPSEEK_API_KEY=your-api-key-here
   ```

3. **Run the app**
   ```bash
   streamlit run chat_pdf.py
   ```

4. **Upload a PDF and start chatting!**

---

**Note**: The `.env` file is shared across all projects in the root directory.
