# 📽️ Chat with YouTube Videos using DeepSeek

RAG application to chat with YouTube videos using DeepSeek API.

## 🚀 Quick Start

1. **Install dependencies**
   ```bash
   pip install -r requirements.txt
   ```

2. **Set API key in `.env` file** (root directory)
   ```env
   DEEPSEEK_API_KEY=your-api-key-here
   ```

3. **Run the app**
   ```bash
   streamlit run chat_youtube.py
   ```

4. **Paste a YouTube URL and start chatting!**

---

**Note**: 
- Videos must have subtitles/transcripts enabled
- The `.env` file is shared across all projects in the root directory
